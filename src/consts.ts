export enum AuthStatus {
  Unknown = 'unknown',
  Auth = 'Auth',
  NoAuth = 'noAuth'
}

export enum APIRoutes {
  Login = '/login',
  Hotels = '/hotels',
  Logout = '/logout',
  Comments = '/comments'
}
